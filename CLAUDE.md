# CLAUDE.md

**All instructions live in [AGENT.md](AGENT.md). Read that first.**

How to write code here: [GUIDELINES.md](GUIDELINES.md).

This file is a pointer only — do not add content here. Keeping one source of
truth means any agent (Claude Code, Cursor, Codex, an editor assistant) reads
the same instructions, rather than each convention drifting in its own file.
If another tool needs its own entry point, add a stub like this one that
redirects here too.
