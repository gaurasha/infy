[package]
name = "infy-kernel"
version = "0.1.0"
edition = "2021"
description = "The types every layer of infy names, and nothing else. serde only."
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
